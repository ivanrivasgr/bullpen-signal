global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]

  # Flink metrics endpoint will be enabled in Phase 4
  # - job_name: flink
  #   static_configs:
  #     - targets: ["flink-jobmanager:9250", "flink-taskmanager:9250"]
